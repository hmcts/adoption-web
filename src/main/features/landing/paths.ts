import { RoutablePath } from 'common/router/routablePath';


export class Paths {
  static readonly landingPage = new RoutablePath('/landing/index')
  static readonly makeNewClaim = new RoutablePath('/landing/make-a-new-claim')
}
